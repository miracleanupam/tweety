<%= provide(:title, "All Users") %>
<div class="center">
  <h1>All Users</h1>
</div>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= paginate @users %>
